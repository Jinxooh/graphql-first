type Movie {
  id: Int!,
  name: String!,
  score: Float!
}

type Query {
  getMovies: [Movie]!,
  getMovieById(id: Int!): Movie!,
}

type Mutation {
  addMovie(name: String!, score: Float!): Movie!
  deleteMovie(id: Int!): Boolean!,
}